# See menu overrides (to disable them) at the bottom of this cfg

### menu resume ###
[menu __resume]
type: list
name: Resume

[menu __resume __resume]
type: command
name: Resume
gcode:
    # RESUME_INTERRUPTED

[menu __resume __cancel]
type: command
name: Cancel
gcode:
    # clear_last_file
    # SAVE_VARIABLE VARIABLE=was_interrupted VALUE=False
    # RUN_SHELL_COMMAND CMD=clear_plr

### menu main ###
[menu __main]
type: list
name: Main

### menu tune ###
[menu __main __tune]
type: list
enable: {printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing"}
name: Tune
index: 1

#[menu __main __tune __get_ip]
#type: command
#name: "Show IP"
#gcode:
#    _GET_IP

# This does not work anymore since we use the default klipper menu and not the Sovol changed menu.py ..
#[menu __main __tune __offsetz]
#type: input
#name: Offset Z:{'%05.2f' % menu.input}
#input: {printer.gcode_move.homing_origin.z}
#input_min: -5
#input_max: 5
#input_step: 0.01
#realtime: True
#gcode:
#    SET_GCODE_OFFSET Z={'%.3f' % menu.input} MOVE=1
#    {% set offadjust = printer.save_variables.variables.offsetadjust|float %}
#    SAVE_VARIABLE VARIABLE=offsetadjust VALUE={'%05.2f' % (offadjust + menu.input - printer.gcode_move.homing_origin.z)}

[menu __main __tune __speed]
type: input
name: Speed: {'%3d' % (menu.input*100)}%
input: {printer.gcode_move.speed_factor}
input_min: 0.01
input_max: 5
input_step: 0.01
realtime: True
gcode:
    M220 S{'%d' % (menu.input*100)}

[menu __main __tune __flow]
type: input
name: Flow: {'%3d' % (menu.input*100)}%
input: {printer.gcode_move.extrude_factor}
input_min: 0.01
input_max: 2
input_step: 0.01
realtime: True
gcode:
    M221 S{'%d' % (menu.input*100)}

[menu __main __tune __fanspeed]
type: input
name: Fan speed: {'%3d' % (menu.input*100)}%
input: {printer["fan"].speed}
input_min: 0
input_max: 1
input_step: 0.01
gcode:
    M106 S{'%d' % (menu.input*255)}

[menu __main __tune __hotend0_target]
type: input
enable: {('extruder' in printer) and ('extruder' in printer.heaters.available_heaters)}
name: {"Ex0:%3.0f (%4.0f)" % (menu.input, printer.extruder.temperature)}
input: {printer.extruder.target}
input_min: 0
input_max: {printer.configfile.config.extruder.max_temp}
input_step: 1
gcode: 
    M104 T0 S{'%.0f' % menu.input}

[menu __main __tune __hotend1_target]
type: input
enable: {('extruder1' in printer) and ('extruder1' in printer.heaters.available_heaters)}
name: {"Ex1:%3.0f (%4.0f)" % (menu.input, printer.extruder1.temperature)}
input: {printer.extruder1.target}
input_min: 0
input_max: {printer.configfile.config.extruder1.max_temp}
input_step: 1
gcode: 
    M104 T1 S{'%.0f' % menu.input}

[menu __main __tune __hotbed_target]
type: input
enable: {'heater_bed' in printer}
name: {"Bed:%3.0f (%4.0f)" % (menu.input, printer.heater_bed.temperature)}
input: {printer.heater_bed.target}
input_min: 0
input_max: {printer.configfile.config.heater_bed.max_temp}
input_step: 1
gcode: 
    M140 S{'%.0f' % menu.input}

[menu __main __tune __exhaustfanonoff]
type: command
name: Exhaust Fan {'ON' if printer['fan_generic exhaust_fan'].speed > 0 else 'OFF'}
enable: {'fan_generic exhaust_fan' in printer}
gcode:
    {% if printer['fan_generic exhaust_fan'].speed > 0 %}
      SET_FAN_SPEED FAN=exhaust_fan SPEED=0
    {% else %}
      SET_FAN_SPEED FAN=exhaust_fan SPEED=1
    {% endif %}

[menu __main __tune __exhaustfanspeed]
type: input
name: Exhaust Fan:{'%3d' % (menu.input*100)}%
input: {printer["fan_generic exhaust_fan"].speed}
input_min: 0
input_max: 1
input_step: 0.01
gcode:
    SET_FAN_SPEED FAN=exhaust_fan SPEED={menu.input}

# yay dimming - MON5TERMATT
[menu __main __tune __ledonoff]
type: input
enable: {'led main_led' in printer}
name: Dim LED:    {'%3d%s' % (menu.input*100,'%') if menu.input else 'OFF'}
input: {printer['led main_led'].color_data[0][3]}
input_min: 0.0
input_max: 1.0
input_step: 0.01
gcode: SET_LED LED=main_led WHITE={menu.input}

[menu __main __tune __save]
type: disabled

[menu __main __tune __save __save_config]
type: disabled

[menu __main __filament1]
type: list
enable: {('virtual_sdcard' in printer) and (printer.print_stats.state == "printing" or printer.print_stats.state == "paused")}
name: {'Runout recovery' if printer.print_stats.state == "paused" else 'Change filament'}
index: 2

[menu __main __filament1 __load]
type: command
enable: {printer.print_stats.state == "paused" and not printer['gcode_macro _FILAMENT_LOAD'].active}
name: Heat for loading
gcode:
    LOAD_FILAMENT
    {menu.exit()}

[menu __main __filament1 __confirm]
type: command
enable: {printer.print_stats.state == "paused" and printer['gcode_macro _FILAMENT_LOAD'].active}
name: Confirm loaded
gcode:
    CONFIRM_FILAMENT_LOADED
    {menu.exit()}

[menu __main __filament1 __cancel_load]
type: command
enable: {printer.print_stats.state == "paused" and printer['gcode_macro _FILAMENT_LOAD'].active}
name: Cancel loading
gcode:
    CANCEL_FILAMENT_LOAD
    {menu.exit()}

[menu __main __filament1 __unload]
type: command
enable: {printer.print_stats.state == "printing"}
name: Start filament change
gcode:
    M600
    {menu.exit()}

[menu __main __resume]
type: command
enable: {('virtual_sdcard' in printer) and printer.print_stats.state == "paused"}
name: Resume printing
index: 3
gcode:
    M117 Resume printing
    {% if "pause_resume" in printer %}
        RESUME
    {% else %}
        M24
    {% endif %}
    {menu.exit()}

[menu __main __pause]
type: command
enable: {('virtual_sdcard' in printer) and printer.print_stats.state == "printing"}
name: Pause printing
index: 3
gcode:
    M117 Pause printing
    {% if "pause_resume" in printer %}
        PAUSE
    {% else %}
        M25
    {% endif %}
    {menu.exit()}

[menu __main __cancel]
type: command
enable: {('virtual_sdcard' in printer) and (printer.print_stats.state == "printing" or printer.print_stats.state == "paused")}
name: Cancel printing
index: 3
gcode:
    # clear_last_file
    # SAVE_VARIABLE VARIABLE=was_interrupted VALUE=False
    # RUN_SHELL_COMMAND CMD=clear_plr
    {% if 'pause_resume' in printer %}
        CANCEL_PRINT
    {% else %}
        M25
        M27
        M26 S0
        TURN_OFF_HEATERS
        {% if printer.toolhead.position.z <= printer.toolhead.axis_maximum.z - 5 %}
            G91
            G0 Z5 F1000
            G90
        {% endif %}
    {% endif %}
    {menu.exit()}

### menu prepare ###
[menu __main __prepare]
type: list
enable: {not (printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing")}
name: Prepare
index: 4

[menu __main __prepare __home]
type: command
name: Auto Home
gcode: HOME_ALL

[menu __main __prepare __preheat_pla]
type: command
enable: {('extruder' in printer) and ('heater_bed' in printer)}
name: Preheat PLA
gcode:
    M117 Preheat PLA
    M140 S65
    M104 S200
    {menu.back()}

[menu __main __prepare __preheat_petg]
type: command
enable: {('extruder' in printer) and ('heater_bed' in printer)}
name: Preheat PETG
gcode:
    M117 Preheat PETG
    M140 S75
    M104 S230
    {menu.back()}

[menu __main __prepare __preheat_abs]
type: command
enable: {('extruder' in printer) and ('heater_bed' in printer)}
name: Preheat ABS
gcode:
    M117 Preheat ABS
    M140 S90
    M104 S260
    {menu.back()}

[menu __main __prepare __cooldown]
type: command
enable: {('extruder' in printer) and ('heater_bed' in printer)}
name: Cooldown
gcode:
    M117 Cooldown
    M104 S0
    M140 S0
    {menu.back()}

### menu filament ###
[menu __main __prepare __hotend0_target]
type: input
enable: {'extruder' in printer}
name: {"Ex0:%3.0f (%4.0f)" % (menu.input, printer.extruder.temperature)}
input: {printer.extruder.target}
input_min: 0
input_max: {printer.configfile.config.extruder.max_temp}
input_step: 1
gcode: 
    M104 T0 S{'%.0f' % menu.input}

### menu levelling ###
[menu __main __levelling]
type: list
enable: {not (printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing")}
name: Levelling
index: 5

[menu __main __levelling __autooffsetz]
type: disabled

[menu __main __levelling __quad_gantry_level]
type: command
enable: {('quad_gantry_level' in printer)}
name: Quad Gantry Lvl
gcode:
    M117 Quad Gantry Lvl
    PREPARE_QGL
    {menu.exit()}

[menu __main __levelling __bed_mesh]
type: command
enable: {('bed_mesh' in printer)}
name: Bed Mesh
gcode: 
    M117 Bed Mesh
    HOME_ALL
    GENERATE_PRINT_MESH ADAPTIVE=0
    {menu.exit()}


### menu virtual sdcard ###
[menu __main __sdcard]
type: vsdlist
enable: {not (printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing")}
name: Print
index: 6

# Keep this row in the populated list so selecting a file with M23 does not
# require leaving and reopening the menu before the start action is available.
[menu __main __sdcard __start]
type: command
enable: {('virtual_sdcard' in printer) and not printer.virtual_sdcard.is_active}
name: {'Start printing' if printer.virtual_sdcard.file_path else 'Select file first'}
gcode:
    {% if printer.virtual_sdcard.file_path %}
        M24
        {menu.exit()}
    {% else %}
        M117 Select a file first
    {% endif %}

### menu control ###
[menu __main __control2]
type: list
enable: {not (printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing")}
name: Control
index: 7

[menu __main __control2 __hotend0_target]
type: input
enable: {('extruder' in printer) and ('extruder' in printer.heaters.available_heaters)}
name: {"Ex0:%3.0f (%4.0f)" % (menu.input, printer.extruder.temperature)}
input: {printer.extruder.target}
input_min: 0
input_max: {printer.configfile.config.extruder.max_temp}
input_step: 1
gcode: 
    M104 T0 S{'%.0f' % menu.input}

[menu __main __control2 __hotbed_target]
type: input
enable: {'heater_bed' in printer}
name: {"Bed:%3.0f (%4.0f)" % (menu.input, printer.heater_bed.temperature)}
input: {printer.heater_bed.target}
input_min: 0
input_max: {printer.configfile.config.heater_bed.max_temp}
input_step: 1
gcode: 
    M140 S{'%.0f' % menu.input}

[menu __main __control2 __fanspeed]
type: input
name: Fan speed: {'%3d' % (menu.input*100)}%
input: {printer["fan"].speed}
input_min: 0
input_max: 1
input_step: 0.01
gcode:
    M106 S{'%d' % (menu.input*255)}

[menu __main __control2 __exhaustfanonoff]
type: command
name: Exhaust Fan {'ON' if printer['fan_generic exhaust_fan'].speed > 0 else 'OFF'}
enable: {'fan_generic exhaust_fan' in printer}
gcode:
    {% if printer['fan_generic exhaust_fan'].speed > 0 %}
      SET_FAN_SPEED FAN=exhaust_fan SPEED=0
    {% else %}
      SET_FAN_SPEED FAN=exhaust_fan SPEED=1
    {% endif %}

[menu __main __control2 __exhaustfanspeed]
type: input
name: Exhaust Fan:{'%3d' % (menu.input*100)}%
input: {printer["fan_generic exhaust_fan"].speed}
input_min: 0
input_max: 1
input_step: 0.01
gcode:
    SET_FAN_SPEED FAN=exhaust_fan SPEED={menu.input}

# yay dimming - MON5TERMATT
[menu __main __control2 __ledonoff]
type: input
enable: {'led main_led' in printer}
name: Dim LED:    {'%3d%s' % (menu.input*100,'%') if menu.input else 'OFF'}
input: {printer['led main_led'].color_data[0][3]}
input_min: 0.0
input_max: 1.0
input_step: 0.01
gcode: SET_LED LED=main_led WHITE={menu.input}

### menu control ###
[menu __main __control]
type: list
enable: {not (printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing")}
name: Move
index: 8

[menu __main __control __disable]
type: command
name: Steppers off
gcode:
    M84
    M18
    {menu.exit()}

[menu __main __control __home]
type: command
name: Auto Home
gcode: 
    HOME_ALL
    {menu.exit()}

### menu move 10mm ###
[menu __main __control __move_10mm]
type: list
name: Move 10mm

[menu __main __control __move_10mm __axis_x]
enable: {'x' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move X:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.x}
input_min: {printer.toolhead.axis_minimum.x}
input_max: {printer.toolhead.axis_maximum.x}
input_step: 10.0
gcode:
    {% if not ('x' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 X{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_10mm __axis_y]
enable: {'y' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move Y:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.y}
input_min: {printer.toolhead.axis_minimum.y}
input_max: {printer.toolhead.axis_maximum.y}
input_step: 10.0
gcode:
    {% if not ('y' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 Y{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_10mm __axis_z]
enable: {'z' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move Z:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.z}
input_min: 0
input_max: {printer.toolhead.axis_maximum.z}
input_step: 10.0
gcode:
    {% if not ('z' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 Z{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_10mm __axis_e]
enable: {printer.extruder.can_extrude and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move E:{'%+06.1f' % menu.input}
input: 0
input_min: -{printer.configfile.config.extruder.max_extrude_only_distance|default(50)}
input_max: {printer.configfile.config.extruder.max_extrude_only_distance|default(50)}
input_step: 10.0
gcode:
    {% if not (printer.extruder.can_extrude and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    M83
    G1 E{menu.input} F240
    RESTORE_GCODE_STATE NAME=__move__axis

### menu move 1mm ###
[menu __main __control __move_1mm]
type: list
name: Move 1mm

[menu __main __control __move_1mm __axis_x]
enable: {'x' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move X:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.x}
input_min: {printer.toolhead.axis_minimum.x}
input_max: {printer.toolhead.axis_maximum.x}
input_step: 1.0
gcode:
    {% if not ('x' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 X{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_1mm __axis_y]
enable: {'y' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move Y:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.y}
input_min: {printer.toolhead.axis_minimum.y}
input_max: {printer.toolhead.axis_maximum.y}
input_step: 1.0
gcode:
    {% if not ('y' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 Y{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_1mm __axis_z]
enable: {'z' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move Z:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.z}
input_min: 0
input_max: {printer.toolhead.axis_maximum.z}
input_step: 1.0
gcode:
    {% if not ('z' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 Z{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_1mm __axis_e]
enable: {printer.extruder.can_extrude and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move E:{'%+06.1f' % menu.input}
input: 0
input_min: -{printer.configfile.config.extruder.max_extrude_only_distance|default(50)}
input_max: {printer.configfile.config.extruder.max_extrude_only_distance|default(50)}
input_step: 1.0
gcode:
    {% if not (printer.extruder.can_extrude and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    M83
    G1 E{menu.input} F240
    RESTORE_GCODE_STATE NAME=__move__axis

### menu move 0.1mm ###
[menu __main __control __move_01mm]
type: list
enable: {not printer.idle_timeout.state == "Printing"}
name: Move 0.1mm

[menu __main __control __move_01mm __axis_x]
enable: {'x' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move X:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.x}
input_min: {printer.toolhead.axis_minimum.x}
input_max: {printer.toolhead.axis_maximum.x}
input_step: 0.1
gcode:
    {% if not ('x' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 X{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_01mm __axis_y]
enable: {'y' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move Y:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.y}
input_min: {printer.toolhead.axis_minimum.y}
input_max: {printer.toolhead.axis_maximum.y}
input_step: 0.1
gcode:
    {% if not ('y' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 Y{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_01mm __axis_z]
enable: {'z' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move Z:{'%05.1f' % menu.input}
input: {printer.gcode_move.gcode_position.z}
input_min: 0
input_max: {printer.toolhead.axis_maximum.z}
input_step: 0.1
gcode:
    {% if not ('z' in printer.toolhead.homed_axes and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    G90
    G1 Z{menu.input}
    RESTORE_GCODE_STATE NAME=__move__axis

[menu __main __control __move_01mm __axis_e]
enable: {printer.extruder.can_extrude and not printer.pause_resume.is_paused and printer.print_stats.state != "printing"}
type: input
name: Move E:{'%+06.1f' % menu.input}
input: 0
input_min: -{printer.configfile.config.extruder.max_extrude_only_distance|default(50)}
input_max: {printer.configfile.config.extruder.max_extrude_only_distance|default(50)}
input_step: 0.1
gcode:
    {% if not (printer.extruder.can_extrude and not printer.pause_resume.is_paused and printer.print_stats.state != "printing") %}
        {action_raise_error("Manual movement unavailable in the current state.")}
    {% endif %}
    SAVE_GCODE_STATE NAME=__move__axis
    M83
    G1 E{menu.input} F240
    RESTORE_GCODE_STATE NAME=__move__axis
    
### Filament Change ###
[menu __main __filament]
type: list
enable: {not (printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing")}
name: Filament
index: 9

[menu __main __filament __load]
type: command
name: Load Filament
gcode:
    {menu.exit()}
    SAVE_GCODE_STATE NAME=__filament__load
    LOAD_FILAMENT
    RESTORE_GCODE_STATE NAME=__filament__load

[menu __main __filament __unload]
type: command
name: Unload Filament
gcode:
    {menu.exit()}
    SAVE_GCODE_STATE NAME=__filament__load
    UNLOAD_FILAMENT
    RESTORE_GCODE_STATE NAME=__filament__load

### menu setup ###
[menu __main __setup]
type: list
enable: {not (printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing")}
name: Advanced
index: 10

#[menu __main __setup __get_ip]
#type: command
#name: "Show IP"
#gcode:
#    _GET_IP

[menu __main __setup __calib]
type: list
name: Calibration

[menu __main __setup __calib __bedprobe]
type: disabled

[menu __main __setup __calib __autocalib]
type: command
name: Auto-calibration
gcode:
    AUTO_CALIBRATE
    {menu.exit()}

[menu __main __setup __calib __belt_test]
type: command
name: Belt resonance
gcode:
    BELT_RESONANCES
    {menu.exit()}

[menu __main __setup __calib __multi_pid]
type: command
name: Multi PID Calibrate
gcode:
    MULTI_PID_CALIBRATE
    {menu.exit()}

# These native tools require manual preparation; no automatic home or save.
# Frequency calibration uses the console paper test (TESTZ / ACCEPT).
# Tap calibration requires a clean/cool nozzle near bed center, 3-10mm above it.
[menu __main __setup __calib __eddy]
type: list
enable: {'probe_eddy_current eddy' in printer}
name: Eddy

[menu __main __setup __calib __eddy __frequency]
type: command
name: Frequency calibration
gcode:
    PROBE_EDDY_CURRENT_CALIBRATE CHIP=eddy
    {menu.exit()}

[menu __main __setup __calib __eddy __tap_guess]
type: command
name: Tap guess
gcode:
    PROBE_EDDY_CURRENT_TAP_CALIBRATE TAP=guess
    {menu.exit()}

[menu __main __setup __calib __eddy __tap_refine]
type: command
name: Tap refine
gcode:
    PROBE_EDDY_CURRENT_TAP_CALIBRATE TAP=refine
    {menu.exit()}

[menu __main __setup __calib __eddy __tap_verify]
type: command
name: Tap verify
gcode:
    PROBE_EDDY_CURRENT_TAP_CALIBRATE TAP=verify
    {menu.exit()}

#[menu __main __setup __timelapse_toggle]
#type: input
#name: Timelapse: {'ON ' if menu.input else 'OFF'}
#input: {printer['gcode_macro TIMELAPSE_TAKE_FRAME'].enable}
#input_min: 0
#input_max: 1
#input_step: 1
#gcode:
#    SET_GCODE_VARIABLE MACRO=TIMELAPSE_TAKE_FRAME VARIABLE=enable VALUE={1 if menu.input else 0}

[menu __main __setup __tuning]
type: command
name: PID tuning
gcode: 
    M117 Tune Hotend PID
    PID_CALIBRATE HEATER=extruder TARGET=210 WRITE_FILE=1
    M117 Tune Hotbed PID
    PID_CALIBRATE HEATER=heater_bed TARGET=60 WRITE_FILE=1
    SAVE_CONFIG

# rename restart menu
[menu __main __setup __restart]
type: list
name: Restart & Shutdown

# add the shutdown option to the restart & shutdown menu
[menu __main __setup __restart __host_shutdown]
type: command
enable: {not printer.idle_timeout.state == "Printing"}
name: Shutdown host
gcode: 
    {action_call_remote_method("shutdown_machine")}

#[menu __main __setup __obico_link]
#type: list
#name: {'Re-link' if printer["gcode_macro OBICO_LINK_STATUS"].is_linked else 'Link'} Obico

#[menu __main __setup __obico_link _install_obico]
#type: command
#enable: { not printer["gcode_macro OBICO_LINK_STATUS"].is_linked and printer["gcode_macro OBICO_LINK_STATUS"].one_time_passcode == ''}
#name: Install Obico
#gcode:
#    M117 Install Obico

#[menu __main __setup __obico_link _one_time_passcode]
#type: command
#enable: { not printer["gcode_macro OBICO_LINK_STATUS"].is_linked and printer["gcode_macro OBICO_LINK_STATUS"].one_time_passcode != ''}
#name: Code: {printer["gcode_macro OBICO_LINK_STATUS"].one_time_passcode}
#gcode:
#    M117 Code: {printer["gcode_macro OBICO_LINK_STATUS"].one_time_passcode}

#[menu __main __setup __obico_link _relink]
#type: command
#enable: {printer["gcode_macro OBICO_LINK_STATUS"].is_linked}
#name: Confirm Re-link
#gcode:
#    _OBICO_RELINK

[menu __main __info]
type: list
enable: {not (printer.print_stats.state == "paused" or printer.idle_timeout.state == "Printing")}
name: Information

[menu __main __info __version_name]
type: command
name: SV08 Mainline Klipper
gcode:

[menu __main __info __menu_version]
type: command
name: Menu cfg: v0.1b
gcode:

[menu __main __info __hostname]
type: command
name: Host: {printer.network_status.hostname}
gcode:

[menu __main __info __klipper_version]
type: command
name: Klipper: {printer.network_status.klipper_version}
gcode:

[menu __main __info __mcu_version]
type: command
name: MCU: {printer.network_status.mcu_version}
gcode:

[menu __main __info __extra_mcu_version]
type: command
name: Extra MCU: {printer.network_status.extra_mcu_version}
gcode:

#[menu __main __info __factory]
#type: list
#name: Factory resets

#[menu __main __info __factory __confirm]
#type: command
#name: Confirm
#gcode:
#    RUN_SHELL_COMMAND CMD=FACTORY_RESETS

# Opening this submenu emits menu:__main __network:populate.  The replacement
# network_status.py listens for that event and refreshes asynchronously.
[menu __main __network]
type: list
name: Network

[menu __main __network __status]
type: command
name: {'Updating...' if printer.network_status.refreshing else ('Moonraker: OK' if printer.network_status.moonraker_ok else 'Moonraker: stale')}
gcode:

[menu __main __network __hostname]
type: command
name: Host: {printer.network_status.hostname}
gcode:

[menu __main __network __mdns]
type: command
name: mDNS: {printer.network_status.mdns}
gcode:

[menu __main __network __active]
type: command
name: Active: {printer.network_status.active_interface} {printer.network_status.active_ip}
gcode:

[menu __main __network __ethip]
type: command
name: Eth IP: {printer.network_status.ethip}
gcode:

[menu __main __network __ethmac]
type: command
name: Eth MAC: {printer.network_status.ethmac}
gcode:

[menu __main __network __wifissid]
type: command
name: WiFi SSID: {printer.network_status.wifissid}
gcode:

[menu __main __network __wifiip]
type: command
name: WiFi IP: {printer.network_status.wifiip}
gcode:

[menu __main __network __wifimac]
type: command
name: WiFi MAC: {printer.network_status.wifimac}
gcode:

[menu __main __network __moonraker]
type: command
name: Moonraker: {printer.network_status.moonraker_version}
gcode:

[menu __main __network __refresh]
type: command
name: Refresh network
gcode:
    NETWORK_STATUS_REFRESH

# DISABLED DEFAULT MENU ITEMS
# we disable some of the default menu items, we now have others or improved ones
[menu __main __octoprint]
type: disabled

[menu __main __temp]
type: disabled

[menu __main __filament __hotend0_target]
type: disabled

[menu __main __filament __loadf]
type: disabled

[menu __main __filament __loads]
type: disabled

[menu __main __filament __unloadf]
type: disabled

[menu __main __filament __unloads]
type: disabled

[menu __main __filament __feed]
type: disabled
