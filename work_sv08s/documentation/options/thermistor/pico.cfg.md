<!-- Generated by _tools/generate.py; edit annotations or config, not this page. -->
# options/thermistor/pico.cfg

Not included by a printer entry point; optional configuration or saved runtime data.

[Source file](../../../config/options/thermistor/pico.cfg) · [All files](../../README.md) · [Reading guide](../../READING_GUIDE.md)

Source text SHA256 (LF-normalized): `df199de696d4ac46cd378ac1cafbe8827ca926bd3b9af7cc8c369ec3e780056f`.

## Macro and action index

- [gcode_macro _QUERY_BME280](#gcode_macro-_query_bme280)

## Other configuration sections

- <code>mcu pico</code> — [source line 1](../../../config/options/thermistor/pico.cfg#L1).
- <code>temperature_sensor chamber</code> — [source line 5](../../../config/options/thermistor/pico.cfg#L5).

<a id="gcode_macro-_query_bme280"></a>

## gcode_macro _QUERY_BME280

Optional sensor-reporting helper in the inactive pico configuration. Reads temperature, pressure and humidity and prints them; it does not control heating.

| Source line | Code | Plain explanation |
| --- | --- | --- |
| [33](../../../config/options/thermistor/pico.cfg#L33) | <code>[gcode_macro _QUERY_BME280]</code> | Declare this callable macro. |
| [34](../../../config/options/thermistor/pico.cfg#L34) | <code>gcode:</code> | Begin the command template. The following indented lines belong to it. |
| [35](../../../config/options/thermistor/pico.cfg#L35) | <code>{% set sensor = printer[&quot;bme280 chamber&quot;] %}</code> | Calculate local <code>sensor</code> from <code>printer[&quot;bme280 chamber&quot;]</code>. It lasts for this evaluation only. |
| [36](../../../config/options/thermistor/pico.cfg#L36) | <code>{action_respond_info(</code> | Write the shown message to the console during template evaluation; this is not a movement. |
| [37](../../../config/options/thermistor/pico.cfg#L37) | <code>&quot;Temperature: %.2f C\n&quot;</code> | Continue the expression or value started above: <code>&quot;Temperature: %.2f C\n&quot;</code>. This is not a separate G-code command. |
| [38](../../../config/options/thermistor/pico.cfg#L38) | <code>&quot;Pressure: %.2f hPa\n&quot;</code> | Continue the expression or value started above: <code>&quot;Pressure: %.2f hPa\n&quot;</code>. This is not a separate G-code command. |
| [39](../../../config/options/thermistor/pico.cfg#L39) | <code>&quot;Humidity: %.2f%%&quot; % (</code> | Continue the expression or value started above: <code>&quot;Humidity: %.2f%%&quot; % (</code>. This is not a separate G-code command. |
| [40](../../../config/options/thermistor/pico.cfg#L40) | <code>sensor.temperature,</code> | Continue the expression or value started above: <code>sensor.temperature,</code>. This is not a separate G-code command. |
| [41](../../../config/options/thermistor/pico.cfg#L41) | <code>sensor.pressure,</code> | Continue the expression or value started above: <code>sensor.pressure,</code>. This is not a separate G-code command. |
| [42](../../../config/options/thermistor/pico.cfg#L42) | <code>sensor.humidity))}</code> | Continue the expression or value started above: <code>sensor.humidity))}</code>. This is not a separate G-code command. |
